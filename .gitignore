# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*.local

# Build-time generated config (contains env secrets)
main/env-config.js

# Claude Code
.claude/

# TypeScript build cache
*.tsbuildinfo

# Electron Builder Output
release/
out/

# Build artifacts (except macOS entitlements and icon)
build/*
!build/entitlements.mac.plist
!build/icon.png
!build/icon.svg
!build/trayIcon.png
!build/trayIcon@2x.png
!build/trayIconTemplate.png
!build/trayIconTemplate@2x.png
supabase/.temp/

# Next.js build output
.next/
next-env.d.ts

# CAPTCHA training data (large local-only artifacts)
training-data/

# CRNN model dosyaları — local'de bundle'a girer ama git'te tutulmaz
# (electron-builder.yml extraResources'tan kopyalar)

# CRNN ensemble seeds (production tek seed2 kullanıyor)
main/automation/models/captcha-v1-seed1.int8.onnx
main/automation/models/captcha-v1-seed3.int8.onnx

# Local-only build/test artifacts root'a sızmış (gerçek kaynaklar main/ altında)
/aiProxy.js
/env-config.js
/eng.traineddata

# ddddocr fine-tune deneme — plateau %81.25, vazgeçildi (CRNN seed2 ile aynı)
ml-training/dddd_trainer/
ml-training/RUNBOOK_DDDDOCR.md
scripts/test-ddddocr.js
scripts/convert-to-ddddocr-format.js

# 2Captcha calibration test result (local only)
2captcha-calibration-result.json
.superpowers/
